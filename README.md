this branch is an effort to use the v3.4 kernel from xiaomi for **CM10**,
but failed with no graphic available

you should update some files from latest MIUI before build
* `prebuilt/kernel`
* `prebuilt/bcmdhd.ko`
* `ramdisk/sbin/chargeonlymode`

`jb-k3.4` branch may help

